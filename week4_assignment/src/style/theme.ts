import { DefaultTheme } from 'styled-components';

const colors = {
  mainColor: '#6D4C41',  
  accentColor: '#A1887F',  
  background: '#FAFAFA',
  textColor: '#333333',
  error: '#FF5252',
};

export const theme: DefaultTheme = {
  colors,
};

export default theme;
